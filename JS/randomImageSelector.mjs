import {animalImages} from "./dataObjects.mjs";

export function randomizeImages(numOfTiles, chosenOptgroup, chosenOption) {
  const uniqueNumOfImages = numOfTiles / 2;
  const radomizedImages = new Array(numOfTiles);
  const radomizedIndices = new Array(numOfTiles);
  //zzz generate rando nums, get the strings, and then return the array. Make sure to consider that
  // we need to duplicate each rando image once.
  function generateRandomIndices (numOfTiles) {

  }
  // animalImages[chosenOptgroup][chosenOption]
}

// TODO: have the numbers duplicate, and find their placement.
